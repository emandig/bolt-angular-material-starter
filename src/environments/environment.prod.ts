import { version } from '../../package.json';

export const environment = {
  appName: 'Bolt - Angular Material Quick Starter',
  production: true,
  envName: 'PROD',
  versions: {
    app: version
  }
};
